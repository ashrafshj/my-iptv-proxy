<?php
// stream.php - Cloud Proxy Script

$id = isset($_GET['id']) ? $_GET['id'] : '900';
$baseUrl = "https://jp.ipl2026.workers.dev/?id=" . $id;

$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, $baseUrl);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_FOLLOWLOCATION, true);
curl_setopt($ch, CURLOPT_USERAGENT, 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36');
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Referer: https://jp.ipl2026.workers.dev/',
    'Origin: https://jp.ipl2026.workers.dev'
]);

$response = curl_exec($ch);
curl_close($ch);

if (preg_match('/https:\/\/s\.ipl2026\.workers\.dev\/[^\s"\']+/i', $response, $matches)) {
    $streamUrl = $matches[0];
} else {
    $streamUrl = "https://s.ipl2026.workers.dev/live.m3u8?id=" . $id;
}

$ch2 = curl_init();
curl_setopt($ch2, CURLOPT_URL, $streamUrl);
curl_setopt($ch2, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch2, CURLOPT_FOLLOWLOCATION, true);
curl_setopt($ch2, CURLOPT_USERAGENT, 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)');
curl_setopt($ch2, CURLOPT_HTTPHEADER, [
    'Referer: https://jp.ipl2026.workers.dev/'
]);

$playlist = curl_exec($ch2);
curl_close($ch2);

header('Content-Type: application/vnd.apple.mpegurl');
header('Access-Control-Allow-Origin: *');
echo $playlist;
?>
